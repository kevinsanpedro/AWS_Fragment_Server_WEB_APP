# docker-compose.yml
services:
  fragments:
    image: kevin9806/fragments:latest
    init: true
    ports:
      - '80:8080'
    environment:
      - NODE_ENV=production
      - HTPASSWD_FILE=tests/.htpasswd
